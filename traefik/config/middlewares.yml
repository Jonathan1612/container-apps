http:
  middlewares:

    # Redirección HTTP → HTTPS
    https-redirect:
      redirectScheme:
        scheme: https
        permanent: true

    # Cabeceras de seguridad (aplica a todas las apps)
    secure-headers:
      headers:
        stsSeconds: 63072000
        stsIncludeSubdomains: true
        stsPreload: true
        forceSTSHeader: true
        contentTypeNosniff: true
        browserXssFilter: true
        referrerPolicy: "strict-origin-when-cross-origin"
        permissionsPolicy: "camera=(), microphone=(), geolocation=()"
        customFrameOptionsValue: "SAMEORIGIN"

    # Igual que secure-headers pero permite cámara (apps con escaneo QR)
    secure-headers-camera:
      headers:
        stsSeconds: 63072000
        stsIncludeSubdomains: true
        stsPreload: true
        forceSTSHeader: true
        contentTypeNosniff: true
        browserXssFilter: true
        referrerPolicy: "strict-origin-when-cross-origin"
        permissionsPolicy: "camera=(self), microphone=(), geolocation=()"
        customFrameOptionsValue: "SAMEORIGIN"

    # Rate limiting — protección contra abuso
    rate-limit:
      rateLimit:
        average: 100
        burst: 50
        period: 1s

    # Compresión gzip
    compress:
      compress: {}
